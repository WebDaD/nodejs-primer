{
  "name": "nodejs-primer",
  "version": "1.0.0",
  "description": "This is a primer. In this description add a short description of the project",
  "main": "index.js",
  "scripts": {
    "test": "istanbul cover _mocha -- tests/*.js -R mochawesome && cp -a mochawesome-report/. dist/public/mochawesome-report/ && cp -a coverage/. dist/public/coverage/",
    "lint": "standard \"index.js\" \"libs/*.js\"",
    "raml":"raml2html docs/api.raml > docs/api.html",
    "jsdoc":"jsdoc lib/* index.js -d docs/jsdoc/",
    "diagrams":"puml generate docs/classes.puml -o docs/classes.png && puml generate docs/components.puml -o docs/components.png",
    "all":"npm run lint && build && npm run test && npm run raml && npm run jsdoc && npm run diagrams",
    "build":"tsc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://TFS"
  },
  "author": "Name",
  "license": "Unlicense",
  "dependencies": {
    "jsonfile": "*",
    "express": "*",
    "http-status": "*"
  },
  "devDependencies": {
    "ip": "*",
    "rimraf": "*",
    "superagent": "^3.6.1",
    "@types/chai": "^4.1.0",
    "@types/mocha": "^2.2.46",
    "chai": "^4.1.2",
    "istanbul": "^0.4.5",
    "jsdoc": "^3.5.5",
    "mocha": "^4.1.0",
    "mochawesome": "^3.0.1",
    "typescript": "^2.6.2"
  }
}
